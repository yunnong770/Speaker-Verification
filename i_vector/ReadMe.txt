Inside i_vector folder, the script i_vector_blind_testing is for blink testing which loads pre-trained parameters to test the performance on unknown testing pairs. The name of the testing file can be modified in the section "Hyperparameter". The script i_vector is the original training and testing script at the time of development.

